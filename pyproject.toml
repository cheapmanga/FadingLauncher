[project]
name = "fading-echo-launcher"
version = "1.0.0"
description = "Launcher Fading Echo pour speedrunners et glitch hunters"
requires-python = ">=3.12"
dependencies = ["PySide6>=6.6"]

[project.scripts]
fe-launcher = "fe_launcher.app:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
